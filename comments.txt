        // Register EncodingManager as ApplicationContextInitializer
        // This ensures encoding is set up before any other beans are initialized
    // EmbeddingResult embed(String text); // Commented out until EmbeddingResult is defined or needed
        // Workaround: Use call() instead of stream() to avoid MessageAggregator issues with tool calls
        // in Spring AI 1.0.0-M6. This ensures tools are executed correctly without crashing.
        // Register fatal error handler
        // Register shutdown hook for graceful exit
        // Display welcome banner
        // Start input loop
                // Get current session
                // Read user input
                // Check for exit signal (Ctrl+C or Ctrl+D)
                // Skip empty input
                // Parse command
                // Execute command
                // Explicit command (starts with /)
                // Non-command input defaults to chat
            // Look up command
                // Command not found - suggest similar commands
            // Check if command requires a session
            // Build command context
            // Execute command
            // Block and wait for completion (synchronous execution in CLI context)
            // Use global exception handler for all command execution errors
            // Save all sessions
            // Display exit message
            // Use exception handler to display error
                // Save all sessions before exit
            // Exit with error code
                    // Save all sessions
            // Register by command name
            // Register by all aliases
                .filter(cd -> cd.distance <= 3) // Only suggest if distance <= 3
        // Create a 2D array to store distances
        // Initialize base cases
        // Fill the dp table
                            dp[i - 1][j],      // deletion
                                    dp[i][j - 1],      // insertion
                                    dp[i - 1][j - 1]   // substitution
        // Extract message from arguments
        // Get services from context
        // Call AgentCore.process() and stream the response
                    // Stream each chunk to the terminal
                    // Add newline after response completes
                    // Handle errors and display error message
            // If no arguments, check if rawInput is a non-command message
                // This is a non-command message (default chat)
            // No message provided
        // Join all arguments into a single message
        // Get renderer from context
            // Clear the screen using the renderer
            // Parse value based on key type
            // Set the configuration value
            // Save all sessions
            // Shutdown session manager
            // Display farewell message
            // Close the Spring application context to trigger shutdown
            // This will cause the application to exit gracefully
        // Check if help for a specific command is requested
        // Display help for all commands
            // Display each command
        // Format: /command (alias1, alias2) - Description
        // Extract optional limit from arguments
        // Get services from context
            // Get session history
            // Handle empty history
            // Apply limit
            // Display header
            // Display messages with color coding
            return Math.min(limit, 1000); // Cap at 1000 messages
        // Format: [HH:mm:ss] Role: Content
            // User messages in blue
            System.out.print("\u001B[34m"); // Blue
            System.out.print("\u001B[0m"); // Reset
            // AI messages in green
            System.out.print("\u001B[32m"); // Green
            System.out.print("\u001B[0m"); // Reset
            // System messages in gray
            System.out.print("\u001B[90m"); // Gray
            System.out.print("\u001B[0m"); // Reset
        // Get services from context
            // Get all sessions
            // Handle empty session list
            // Get current session to mark it
            // Mark current session in the list
            // Format and display the table
            // Display summary
        // Extract optional session name from arguments
        // Get services from context
            // Create new session
            // Switch to the new session
            // Display success message
            // Generate default name with timestamp
        // Join all arguments into a single session name
        // Extract session identifier from arguments
        // Get services from context
            // Find session by ID or name
            // Check if already on this session
            // Switch to the target session
            // Display success message
            // Display message count
        // Join all arguments into a single identifier (supports names with spaces)
        // Try exact ID match first
        // Try partial ID match (prefix)
        // Try exact name match
        // Try case-insensitive name match
            // Application version
            // Java information
            // Operating system information
            // Runtime information
                    // Windows系统默认使用GBK编码
                    // Linux和macOS系统使用UTF-8编码
                // Console对象存在，尝试获取其编码
        // Ctrl+C: 中断当前操作（默认已支持）
        // Ctrl+D: 退出应用（默认已支持，会返回null）
        // Ctrl+L: 清空屏幕
                .bind(new org.jline.reader.Reference("clear-screen"), "\u000C"); // Ctrl+L
        // Home/End键（默认已支持）
        // Install Jansi to enable ANSI support on Windows
        // ANSI escape code to clear screen and move cursor to home position
        // Top border - 使用ASCII字符以确保跨平台兼容
        // Title
        // Version
        // Separator
        // Usage tips
        // Bottom border
        // Add header row
        // Add session rows
        // Set column alignment
        // Add header row
        // Add data rows
        // Set column alignment
     * @see <a href="https://en.wikipedia.org/wiki/ANSI_escape_code">ANSI Escape Codes</a>
            // Only enforce minimum width for auto-detected terminals
            // Allow explicit width for testing purposes
        // Split by existing newlines to preserve intentional line breaks
        // Split into words while preserving whitespace
            // Handle very long words that exceed terminal width
                // Flush current line if not empty
                // Break long word at terminal width boundaries
            // Check if adding this word would exceed terminal width
                // Flush current line (trim trailing whitespace)
                // Start new line with current word (skip leading whitespace)
        // Append remaining content
            // Not a command, treat as chat message
        // Extract command name
        // Extract arguments
        // Parse arguments into positional, named, and flags
                // Check if it's a named argument (--key=value)
                    // It's a flag (--flag)
                // Positional argument
    // Cache fields
        // Check cache first (double-checked locking pattern)
            // Double-check after acquiring lock
            // Update cache
            // Validate before saving
            // Ensure directory exists
            // Write to file
            // Update cache
        // Validate history size
        // Validate auto save interval
        // Validate terminal width
        // Validate default session name is not empty
        // Log detailed error information
        // Determine error category and display appropriate message
        // Display stack trace in debug mode
        // Suggest similar commands if command not found
        // Display exception class and message
        // Display stack trace elements
        // Display cause if present
        // Log fatal error
        // Display simple error message (avoid complex rendering that might fail)
        // Display stack trace if available
            // Ignore config loading errors during fatal error handling
            // Read first line
            // Handle multi-line input (backslash continuation)
                // Remove trailing whitespace and backslash
                // Read next line with continuation prompt
                    // EOF during multi-line input
                // Append with a space separator
            // Ctrl+C pressed - return null to signal interruption
            // Ctrl+D pressed - return null to signal EOF/exit
    // In-memory cache of sessions
            // Create sessions directory
            // Load existing sessions
            // Create default session if no sessions exist
                // Set first session as current
        // Update previous session
        // Update new session
        // Delete session file
        // Load messages from MemoryService
        // Convert to Message objects (simplified)
        // We use a StringBuilder to accumulate the full response for saving to memory
        // Store in user home directory under .openbutler/conversations
        // Simple implementation: return empty for now
        // TODO: Implement preferences storage
                // Ignore error, create new
                // Ignore error
        // Register default tools
            // Set working directory to current user directory
