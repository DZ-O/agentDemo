spring:
  main:
    web-application-type: none
  application:
    name: OpenButler
  ai:
    openai:
      api-key:
      chat:
        options:
          model:
      base-url:

logging:
  level:
    root: debug
    com.openbutler: debug
    org.springframework.ai.chat.client.advisor: debug
    org.springframework.ai.chat.model.openai: debug
    org.springframework.ai.model.tool: debug
